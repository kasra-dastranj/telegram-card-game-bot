# 🎮 راهنمای نصب و راه‌اندازی کامل
## ربات بازی کارت تلگرام - فاز ۱

---

## 📋 فهرست

1. [پیش‌نیازها](#پیش-نیازها)
2. [نصب سیستم](#نصب-سیستم)
3. [دریافت توکن ربات](#دریافت-توکن-ربات)
4. [تنظیمات](#تنظیمات)
5. [اضافه کردن عکس‌های کارت](#اضافه-کردن-عکس-های-کارت)
6. [اجرای ربات](#اجرای-ربات)
7. [تست سیستم](#تست-سیستم)
8. [مشکلات متداول](#مشکلات-متداول)
9. [نگهداری سیستم](#نگهداری-سیستم)

---

## 🔧 پیش‌نیازها

### نرم‌افزارهای مورد نیاز:
- **Python 3.8+** (توصیه: Python 3.9 یا بالاتر)
- **pip** (Package installer برای Python)
- **اتصال اینترنت** (برای تلگرام API)

### سیستم‌عامل‌های پشتیبانی شده:
- ✅ Windows 10/11
- ✅ macOS 10.14+
- ✅ Linux (Ubuntu 18.04+, CentOS 7+)

### بررسی Python:
```bash
python --version
# یا
python3 --version
```

اگر Python نصب نیست:
- **Windows**: از [python.org](https://python.org) دانلود کنید
- **macOS**: `brew install python` یا از python.org
- **Ubuntu/Debian**: `sudo apt update && sudo apt install python3 python3-pip`
- **CentOS/RHEL**: `sudo yum install python3 python3-pip`

---

## 💾 نصب سیستم

### مرحله 1: دانلود فایل‌ها
تمام فایل‌های زیر را در یک پوشه قرار دهید:

```
telegram-card-game/
├── game_core.py
├── telegram_bot.py  
├── admin_setup.py
├── requirements.txt
├── run.py
└── README.md
```

### مرحله 2: نصب Dependencies
```bash
cd telegram-card-game
pip install -r requirements.txt

# در صورت مشکل:
pip3 install -r requirements.txt

# یا به صورت دستی:
pip install python-telegram-bot==20.7 Pillow==10.1.0
```

### مرحله 3: راه‌اندازی اولیه
```bash
python admin_setup.py
```

یا

```bash
python run.py
# سپس گزینه 2 (Admin Panel) را انتخاب کنید
```

---

## 🤖 دریافت توکن ربات

### مرحله 1: ساخت ربات
1. **در تلگرام** `@BotFather` را جستجو کنید
2. دستور `/start` را بزنید
3. دستور `/newbot` را بزنید

### مرحله 2: انتخاب نام
```
BotFather: Alright, a new bot. How are we going to call it?
شما: My Card Game Bot

BotFather: Good. Now let's choose a username for your bot.
شما: my_card_game_bot
```

**نکته**: یوزرنیم باید به `bot` ختم شود و منحصربه‌فرد باشد.

### مرحله 3: دریافت توکن
BotFather توکنی شبیه این ارسال می‌کند:
```
123456789:ABCdefGHIjklMNOpqrsTUVwxyz
```

**⚠️ مهم**: این توکن را محرمانه نگه دارید!

### مرحله 4: دریافت User ID خودتان
1. `@userinfobot` را در تلگرام جستجو کنید
2. `/start` بزنید
3. عدد `Id` را یادداشت کنید (مثل: `123456789`)

---

## ⚙️ تنظیمات

### ویرایش فایل تنظیمات
فایل `game_config.json` را باز کنید:

```json
{
  "bot_settings": {
    "token": "123456789:ABCdefGHIjklMNOpqrsTUVwxyz",  // توکن ربات اینجا
    "admin_user_ids": [123456789],  // User ID شما اینجا
    "webhook_url": null,
    "webhook_port": 8443
  },
  "game_settings": {
    "daily_hearts": 5,           // تعداد قلب روزانه
    "heart_reset_hours": 24,     // ریست قلب‌ها (ساعت)
    "claim_cooldown_hours": 24,  // کولدان کلیم کارت
    "ability_cooldown_hours": 24, // کولدان ابیلیتی
    "max_cards_per_page": 8,     // حداکثر کارت در صفحه
    "card_drop_rates": {
      "normal": 65,              // درصد ظهور Normal
      "epic": 25,                // درصد ظهور Epic  
      "legend": 10               // درصد ظهور Legend
    }
  }
}
```

### مثال کامل:
```json
{
  "bot_settings": {
    "token": "1234567890:ABCDEFghijklmnopQRSTUVwxyz123456789",
    "admin_user_ids": [987654321]
  },
  "game_settings": {
    "daily_hearts": 5,
    "heart_reset_hours": 24,
    "claim_cooldown_hours": 24,
    "ability_cooldown_hours": 24,
    "max_cards_per_page": 8,
    "card_drop_rates": {
      "normal": 65,
      "epic": 25,
      "legend": 10
    }
  }
}
```

---

## 🖼️ اضافه کردن عکس‌های کارت

### ساختار پوشه:
```
telegram-card-game/
├── card_images/
│   ├── heisenberg.png
│   ├── spongebob.png
│   ├── homelander.png
│   ├── thanos.png
│   └── ...
```

### کارت‌های پیش‌فرض:
عکس‌های زیر را در پوشه `card_images/` قرار دهید:

**Normal Cards:**
- `heisenberg.png` - والتر وایت
- `spongebob.png` - باب اسفنجی  
- `tr.png` - شخصیت TR
- `kangfupanda.png` - پاندای کونگ فو
- `reza.png` - شخصیت رضا
- `mrbean.png` - مستر بین
- `tataloo.png` - تتلو

**Epic Cards:**
- `homelander.png` - هوم‌لندر
- `hannah_irani.png` - هانا ایرانی

**Legend Cards:**
- `thanos.png` - تانوس

### مشخصات عکس‌ها:
- **فرمت**: PNG (ترجیحی), JPG, JPEG, GIF
- **ابعاد**: 300x400 پیکسل (ترجیحی)  
- **حجم**: کمتر از 5MB
- **پس‌زمینه**: شفاف (برای PNG)
- **نام فایل**: حروف کوچک، بدون فاصله، _ به جای space

### نکات مهم:
- اگر عکسی موجود نباشد، ربات بدون عکس کار می‌کند
- می‌توانید بعداً عکس‌ها را اضافه کنید
- عکس‌ها در دیتابیس ذخیره نمی‌شوند، فقط مسیرشان

---

## 🚀 اجرای ربات

### روش 1: اسکریپت Quick Start
```bash
python run.py
# سپس گزینه 1 (Start Telegram Bot) را انتخاب کنید
```

### روش 2: اجرای مستقیم
```bash
python telegram_bot.py
```

### روش 3: پنل مدیریت
```bash
python admin_setup.py
```

### پیام‌های موفقیت:
```
🤖 ربات در حال راه‌اندازی...
🎮 نام بازی: ربات کارت‌های شخصیت‌ها
📊 تعداد کارت‌های موجود: 10
✅ ربات آماده است! /start را بزنید
```

---

## 🧪 تست سیستم

### تست اولیه:
1. **تست ربات در تلگرام:**
   - ربات خود را در تلگرام پیدا کنید
   - `/start` بزنید
   - باید منوی اصلی نمایش داده شود

2. **تست دریافت کارت:**
   - "🎁 دریافت کارت روزانه" را بزنید
   - باید کارتی دریافت کنید

3. **تست فایت:**
   - "⚔️ شروع فایت" را بزنید
   - کارت و ویژگی انتخاب کنید
   - نتیجه باید نمایش داده شود

### تست پنل مدیریت:
```bash
python admin_setup.py
# گزینه 7: تست سیستم
```

### بررسی فایل‌ها:
- ✅ `game_bot.db` - دیتابیس (حدود 20KB+)
- ✅ `game_config.json` - تنظیمات
- ✅ `card_images/` - پوشه عکس‌ها
- ✅ `bot.log` - فایل لاگ (بعد از اولین اجرا)

---

## 🔧 مشکلات متداول

### مشکل 1: "ModuleNotFoundError"
```bash
ModuleNotFoundError: No module named 'telegram'
```

**راه حل:**
```bash
pip install python-telegram-bot==20.7
# یا
pip3 install python-telegram-bot==20.7
```

### مشکل 2: "Invalid token"
```
telegram.error.InvalidToken: Invalid token
```

**علت‌ها:**
- توکن اشتباه وارد شده
- توکن منقضی شده
- فاصله یا کاراکتر اضافی

**راه حل:**
1. توکن جدید از BotFather دریافت کنید
2. `game_config.json` را دوباره بررسی کنید
3. مطمئن شوید کاراکتر اضافی نداشته باشید

### مشکل 3: Database Locked
```
sqlite3.OperationalError: database is locked
```

**راه حل:**
```bash
# ربات را متوقف کنید (Ctrl+C)
# چند ثانیه صبر کنید
# دوباره اجرا کنید
```

### مشکل 4: Port در استفاده
```
OSError: [Errno 48] Address already in use
```

**راه حل:**
```bash
# پیدا کردن فرآیند
ps aux | grep python

# خاتمه فرآیند
kill -9 [PID]

# یا ریستارت سیستم
```

### مشکل 5: هیچ کارتی موجود نیست
**راه حل:**
```bash
python admin_setup.py
# گزینه 6: راه‌اندازی اولیه
```

### مشکل 6: عکس‌ها نمایش داده نمی‌شوند
**علت‌ها:**
- مسیر فایل اشتباه
- فرمت پشتیبانی نشده
- حجم زیاد

**راه حل:**
1. نام فایل‌ها را بررسی کنید
2. فرمت PNG استفاده کنید
3. حجم کمتر از 5MB
4. حروف کوچک و بدون فاصله

---

## 📱 راه‌اندازی Production

### برای سرور لینوکس:

#### 1. نصب و راه‌اندازی:
```bash
# کلون یا آپلود فایل‌ها
cd /opt/telegram-card-game

# نصب Dependencies
sudo apt update
sudo apt install python3 python3-pip python3-venv
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

#### 2. ایجاد Service:
```bash
sudo nano /etc/systemd/system/cardbot.service
```

محتوای فایل:
```ini
[Unit]
Description=Telegram Card Game Bot
After=network.target

[Service]
Type=simple
User=www-data
WorkingDirectory=/opt/telegram-card-game
Environment=PATH=/opt/telegram-card-game/venv/bin
ExecStart=/opt/telegram-card-game/venv/bin/python telegram_bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

#### 3. فعال‌سازی Service:
```bash
sudo systemctl enable cardbot.service
sudo systemctl start cardbot.service
sudo systemctl status cardbot.service
```

#### 4. مانیتورینگ:
```bash
# مشاهده لاگ‌ها
sudo journalctl -u cardbot.service -f

# ریستارت
sudo systemctl restart cardbot.service

# متوقف کردن
sudo systemctl stop cardbot.service
```

### برای Windows Service:
از ابزارهایی مثل `NSSM` یا `Windows Task Scheduler` استفاده کنید.

---

## 💾 نگهداری سیستم

### بکاپ روزانه:
```bash
# اسکریپت بکاپ (backup.sh)
#!/bin/bash
DATE=$(date +%Y%m%d_%H%M%S)
mkdir -p backups
cp game_bot.db backups/game_bot_$DATE.db
cp game_config.json backups/game_config_$DATE.json
echo "Backup created: $DATE"
```

اجرا:
```bash
chmod +x backup.sh
./backup.sh

# یا از پنل مدیریت:
python admin_setup.py
# گزینه 5: پشتیبان‌گیری
```

### مانیتورینگ:
```bash
# بررسی فضای دیسک
df -h

# بررسی حافظه
free -h

# بررسی CPU
top

# بررسی لاگ‌ها
tail -f bot.log
```

### بروزرسانی:
1. بکاپ دیتابیس و تنظیمات
2. دانلود فایل‌های جدید
3. بررسی تغییرات `game_config.json`
4. ریستارت سیستم
5. تست عملکرد

---

## 📊 آمار و عملکرد

### بررسی آمار:
```bash
python admin_setup.py
# گزینه 3: آمار و گزارشات
```

### مقادیر بهینه:
- **تعداد بازیکن همزمان**: تا 1000 نفر
- **حجم دیتابیس**: حداکثر 100MB
- **مصرف RAM**: 50-100MB
- **مصرف CPU**: کمتر از 5%

### علائم مشکل:
- پاسخ کند ربات (>5 ثانیه)
- خطاهای Database Lock
- مصرف RAM بالا (>500MB)
- خطاهای Connection Timeout

---

## 🎯 نکات نهایی

### بهترین شیوه‌ها:
1. **بکاپ منظم** از دیتابیس
2. **مانیتورینگ** لاگ‌ها
3. **بروزرسانی** dependencies
4. **تست** قابلیت‌های جدید
5. **محدود کردن** دسترسی ادمین

### امنیت:
- توکن ربات را محرمانه نگه دارید
- فایل `game_config.json` را در git قرار ندهید
- دسترسی سرور را محدود کنید
- بکاپ‌ها را رمزنگاری کنید

### عملکرد:
- از SSD برای دیتابیس استفاده کنید
- Connection Pool را تنظیم کنید
- فایل‌های لاگ را rotate کنید
- عکس‌های کارت را optimize کنید

---

## 🎉 تبریک!

اگر تا اینجا آمده‌اید، ربات بازی کارت تلگرام شما آماده است! 

### گام‌های بعدی:
1. **تست کامل** تمام قابلیت‌ها
2. **اضافه کردن** کارت‌ها و عکس‌های بیشتر
3. **دعوت** دوستان برای تست
4. **مانیتور** عملکرد سیستم
5. **آماده‌سازی** برای فاز ۲

**موفق باشید! 🚀**

---

*آخرین بروزرسانی: مهر ۱۴۰۳*
*نسخه: ۱.۰.۰ (فاز ۱)*