# ⚡ راهنمای سریع حل مشکل /fight

## 🎯 وضعیت فعلی
- ✅ ربات بالاست و کار می‌کنه
- ✅ 100 کارت در دیتابیس
- ✅ 17 بازیکن ثبت شده
- ✅ دستور fight ثبت شده
- ❓ ولی /fight کار نمی‌کنه

---

## 🔧 راه‌حل قدم به قدم

### قدم 1: چک کردن Admin بودن ربات (مهم‌ترین!)

**در تلگرام:**
1. برو تو گروهی که می‌خوای تست کنی
2. تنظیمات گروه → Administrators
3. ببین آیا `@TelBattleBot` تو لیست ادمین‌هاست؟

**اگر نیست:**
- Add Administrator کن
- ربات رو انتخاب کن
- این دسترسی‌ها رو بده:
  - ✅ Delete Messages
  - ✅ Invite Users via Link
  - ✅ Pin Messages (اختیاری)

**اگر هست:**
- برو قدم 2

---

### قدم 2: تست دستور در گروه

**در گروه:**
1. تایپ کن: `/`
2. باید لیست دستورات نشون بده
3. ببین `/fight` تو لیسته؟

**اگر لیست دستورات نشون نداد:**
```bash
# روی سرور:
pkill -9 -f telegram_bot.py
nohup python3 telegram_bot.py > bot.log 2>&1 &
```

**اگر لیست نشون داد:**
- برو قدم 3

---

### قدم 3: تست با یوزر خودت

**مطمئن شو:**
1. در پیوی ربات `/start` زدی
2. حداقل 1 کارت داری (با `/claim`)
3. حداقل 1 جان داری

**چک کردن:**
```bash
# روی سرور (USER_ID رو عوض کن):
python3 -c "
from game_core import DatabaseManager, GameLogic
import json

with open('game_config.json') as f:
    config = json.load(f)

db = DatabaseManager()
game = GameLogic(db, config)

user_id = 5735941901  # ID خودت
player = db.get_or_create_player(user_id)
player = game.check_and_reset_hearts(player)
cards = db.get_player_cards(user_id)

print(f'Name: {player.first_name}')
print(f'Hearts: {player.hearts}/{game.DAILY_HEARTS}')
print(f'Cards: {len(cards)}')

if len(cards) == 0:
    print('❌ NO CARDS!')
elif player.hearts <= 0:
    print('❌ NO HEARTS!')
else:
    print('✅ Ready to fight!')
"
```

---

### قدم 4: مانیتور کردن لاگ‌ها

**روی سرور:**
```bash
# آپلود اسکریپت
# (از Windows)
scp "C:\Users\lenovo\Desktop\card game\watch_bot_logs.sh" root@195.248.243.122:"/root/card game/"

# اجرا
ssh root@195.248.243.122
cd "/root/card game"
chmod +x watch_bot_logs.sh
./watch_bot_logs.sh
```

**حالا:**
1. این ترمینال رو باز نگه دار
2. برو تو گروه تلگرام
3. `/fight` بزن
4. ببین چه خطایی تو ترمینال میاد

---

### قدم 5: تست با دو نفر

**نیاز:**
- 2 نفر که هر دو ربات رو استارت کردن
- هر دو حداقل 1 کارت دارن
- هر دو حداقل 1 جان دارن

**تست:**
1. نفر اول: `/fight` بزنه
2. باید پیام چالش بیاد
3. نفر دوم: روی دکمه "قبول" کلیک کنه
4. باید پنل انتخاب کارت بیاد

---

## 🐛 دیباگ پیشرفته

### اگر هیچ پاسخی نمی‌ده:

**چک 1: ربات زنده‌ست؟**
```bash
ps aux | grep telegram_bot.py
```

**چک 2: خطا تو لاگ هست؟**
```bash
tail -100 bot.log | grep -i error
```

**چک 3: ربات به تلگرام وصله؟**
```bash
tail -20 bot.log | grep -i "polling\|update"
```

---

### اگر پیام خطا میاد:

**"ابتدا باید کارتی داشته باشید"**
→ برو پیوی ربات و `/claim` بزن

**"جان شما تمام شده"**
→ از پنل ادمین جان بده یا صبر کن

**"شما قبلاً چالش فعالی دارید"**
→ فایت قبلی رو کامل کن یا منتظر بمون منقضی بشه

---

## 🎬 ویدیوی تست

**مراحل دقیق:**
1. ✅ ربات رو admin کن
2. ✅ در پیوی `/start` بزن
3. ✅ در پیوی `/claim` بزن (اگه کارت نداری)
4. ✅ برو گروه
5. ✅ `/fight` بزن
6. ✅ باید پیام چالش بیاد با 2 دکمه:
   - "✊ قبول (نرمال)"
   - "🎲 قبول (تصادفی)"

---

## 💡 نکات مهم

1. **ربات باید Admin باشه** - این مهم‌ترین شرطه!
2. **هر دو نفر باید ربات رو استارت کرده باشن**
3. **هر دو نفر باید کارت داشته باشن**
4. **هر دو نفر باید جان داشته باشن**

---

## 🆘 اگر باز کار نکرد

**اطلاعات زیر رو بفرست:**

1. **اسکرین‌شات از:**
   - لیست ادمین‌های گروه
   - پیامی که بعد از `/fight` میاد (یا نمیاد)
   - لیست دستورات ربات (وقتی `/` می‌زنی)

2. **خروجی این دستورات:**
```bash
# روی سرور:
tail -50 bot.log
ps aux | grep telegram_bot
```

3. **تست این:**
```bash
# در پیوی ربات:
/start
/claim
/cards

# در گروه:
/fight
```

---

**🎯 احتمال 99% مشکل از Admin نبودن ربات در گروهه!**
