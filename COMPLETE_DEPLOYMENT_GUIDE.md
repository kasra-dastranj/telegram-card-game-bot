# 🚀 راهنمای کامل دیپلوی سیستم

## ✅ تغییرات اعمال شده

### 1. **game_core.py**
- ✅ اضافه شدن `challenger` و `opponent` به نتیجه فایت
- ✅ حل مشکل نمایش "N/A" و "stat 0" در نتایج فایت
- ✅ کارت‌ها با تمام ویژگی‌ها (name, power, speed, iq, popularity) ارسال می‌شوند

### 2. **web_api.py**
- ✅ API جدید: `/api/players` - لیست تمام بازیکنان
- ✅ API جدید: `/api/players/<user_id>` - جزئیات یک بازیکن
- ✅ API جدید: `/api/players/<user_id>/reset-hearts` - ریست جان‌ها
- ✅ API جدید: `/api/pvp/active-fights` - فایت‌های فعال
- ✅ API جدید: `/api/pvp/history` - تاریخچه فایت‌ها
- ✅ API جدید: `/api/pvp/cleanup` - پاکسازی فایت‌های منقضی
- ✅ بهبود `/api/stats` - اضافه شدن آمار PvP

### 3. **admin_panel_complete.html** (جدید!)
- ✅ داشبورد کامل با آمار لحظه‌ای
- ✅ مدیریت کارت‌ها (مشاهده، حذف، ویرایش)
- ✅ مدیریت بازیکنان (مشاهده، ریست جان)
- ✅ مدیریت PvP (فایت‌های فعال، تاریخچه، پاکسازی)
- ✅ تنظیمات بازی (جان روزانه، Cooldown)
- ✅ رابط کاربری مدرن و زیبا

---

## 📋 مراحل دیپلوی

### روش 1: استفاده از اسکریپت خودکار

```powershell
# اجرای فایل دیپلوی
.\deploy_complete.bat
```

### روش 2: دستی

#### مرحله 1: آپلود فایل‌ها

```powershell
# آپلود game_core.py
scp "C:\Users\lenovo\Desktop\card game\game_core.py" root@195.248.243.122:"/root/card game/"

# آپلود web_api.py
scp "C:\Users\lenovo\Desktop\card game\web_api.py" root@195.248.243.122:"/root/card game/"

# آپلود پنل مدیریت جدید
scp "C:\Users\lenovo\Desktop\card game\admin_panel_complete.html" root@195.248.243.122:"/root/card game/"
```

#### مرحله 2: اتصال به سرور

```powershell
ssh root@195.248.243.122
```

#### مرحله 3: ریستارت سرویس‌ها

```bash
cd "/root/card game"

# ریستارت ربات تلگرام
pkill -9 -f telegram_bot.py
nohup python3 telegram_bot.py > bot.log 2>&1 &

# ریستارت پنل مدیریت
pkill -9 -f web_api.py
nohup python3 web_api.py > web.log 2>&1 &

# مشاهده لاگ ربات
tail -f bot.log

# یا مشاهده لاگ پنل
tail -f web.log
```

---

## 🌐 دسترسی به پنل‌ها

### پنل مدیریت جدید (پیشنهادی)
```
http://195.248.243.122:5000/
```

**ویژگی‌ها:**
- 📊 داشبورد کامل
- 🎴 مدیریت کارت‌ها
- 👥 مدیریت بازیکنان
- ⚔️ مدیریت PvP
- ⚙️ تنظیمات

### پنل قدیمی (برای مقایسه)
```
http://195.248.243.122:5000/old
```

---

## 🧪 تست سیستم

### 1. تست ربات تلگرام

1. باز کردن ربات در تلگرام
2. ارسال `/start`
3. شروع یک فایت PvP در گروه
4. بررسی نمایش صحیح نتایج (باید اسم کارت‌ها و مقادیر stat‌ها نمایش داده شود)

### 2. تست پنل مدیریت

1. باز کردن `http://195.248.243.122:5000/`
2. بررسی داشبورد (باید آمار نمایش داده شود)
3. رفتن به بخش "مدیریت بازیکنان"
4. رفتن به بخش "مدیریت PvP"
5. تست ریست جان یک بازیکن

---

## 🔍 عیب‌یابی

### مشکل: ربات کار نمی‌کند

```bash
# چک کردن لاگ
tail -f /root/card\ game/bot.log

# چک کردن پروسس
ps aux | grep telegram_bot.py

# ریستارت دستی
cd "/root/card game"
pkill -9 -f telegram_bot.py
nohup python3 telegram_bot.py > bot.log 2>&1 &
```

### مشکل: پنل مدیریت باز نمی‌شود

```bash
# چک کردن لاگ
tail -f /root/card\ game/web.log

# چک کردن پروسس
ps aux | grep web_api.py

# ریستارت دستی
cd "/root/card game"
pkill -9 -f web_api.py
nohup python3 web_api.py > web.log 2>&1 &
```

### مشکل: نتایج فایت هنوز "N/A" نشان می‌دهد

1. مطمئن شوید `game_core.py` آپلود شده
2. ربات را ریستارت کنید
3. یک فایت جدید شروع کنید (فایت‌های قدیمی ممکن است مشکل داشته باشند)

---

## 📊 API های موجود

### کارت‌ها
- `GET /api/cards` - لیست تمام کارت‌ها
- `POST /api/cards` - افزودن کارت جدید
- `DELETE /api/cards/<id>` - حذف کارت
- `GET /api/cards/<id>/cooldown` - تنظیمات cooldown کارت
- `POST /api/cards/<id>/cooldown` - بروزرسانی cooldown

### بازیکنان
- `GET /api/players` - لیست بازیکنان
- `GET /api/players/<user_id>` - جزئیات بازیکن
- `POST /api/players/<user_id>/reset-hearts` - ریست جان‌ها

### PvP
- `GET /api/pvp/active-fights` - فایت‌های فعال
- `GET /api/pvp/history` - تاریخچه فایت‌ها
- `POST /api/pvp/cleanup` - پاکسازی فایت‌های منقضی

### تنظیمات
- `GET /api/game-settings` - دریافت تنظیمات
- `POST /api/game-settings` - بروزرسانی تنظیمات

### آمار
- `GET /api/stats` - آمار کامل سیستم

---

## ✨ ویژگی‌های جدید

### 1. نمایش صحیح نتایج فایت
- ✅ اسم کارت‌ها به جای "N/A"
- ✅ مقادیر واقعی stat‌ها به جای "stat 0"
- ✅ محاسبه صحیح مجموع امتیازات

### 2. پنل مدیریت پیشرفته
- ✅ داشبورد با آمار لحظه‌ای
- ✅ مدیریت کامل بازیکنان
- ✅ نظارت بر فایت‌های PvP
- ✅ رابط کاربری مدرن و responsive

### 3. API های جدید
- ✅ مدیریت بازیکنان
- ✅ مدیریت PvP
- ✅ آمار تفصیلی

---

## 🎯 نتیجه

بعد از دیپلوی موفق:

1. ✅ ربات تلگرام با تمام فیچرها کار می‌کند
2. ✅ نتایج فایت به درستی نمایش داده می‌شود
3. ✅ پنل مدیریت کامل و حرفه‌ای
4. ✅ API های جامع برای مدیریت
5. ✅ آمار و گزارش‌های دقیق

---

## 📞 پشتیبانی

اگر مشکلی پیش آمد:
1. لاگ‌ها را بررسی کنید
2. مطمئن شوید تمام فایل‌ها آپلود شده‌اند
3. سرویس‌ها را ریستارت کنید
4. یک فایت تست انجام دهید

**موفق باشید! 🚀**
