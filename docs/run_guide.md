# 🚀 راهنمای اجرای بازی کارت تلگرام

## ✅ آماده‌سازی اولیه

### 1. نصب وابستگی‌ها
```bash
pip install -r requirements.txt
```

### 2. آپدیت دیتابیس (اگر لازم باشد)
```bash
python migrate_cooldown_table.py
```

### 3. بررسی وضعیت دیتابیس
```bash
python check_db.py
```

## 🤖 اجرای ربات تلگرام

```bash
python telegram_bot.py
```

خروجی موفق:
```
✅ ربات آماده شد با 2 ادمین
🖼 پوشه‌های تصاویر آماده شد:
   🎴 کارت‌ها: /root/card game/card_images/
✅ 11 کارت در دیتابیس موجود است
```

## 🌐 اجرای پنل مدیریت وب

```bash
python web_api.py
```

خروجی موفق:
```
🌐 Starting Complete Web Management Panel on http://0.0.0.0:5000
 * Running on http://127.0.0.1:5000
```

پنل ادمین در آدرس: http://localhost:5000

## 📋 ویژگی‌های جدید

### ✨ سیستم Cooldown جداگانه برای کارت‌ها
- هر کارت Epic/Legend تنظیمات Cooldown جداگانه دارد
- قابل تنظیم از پنل ادمین:
  - حد مجاز برد (Win Limit)
  - مدت Cooldown (ساعت)
  - فعال/غیرفعال کردن

### 💖 سیستم جان جدید (10 جان)
- تغییر از 5 جان به 10 جان
- قوانین جدید کم شدن جان:
  - Legend شکست از Normal: -2 جان
  - Normal شکست از Legend: 0 جان
  - سایر موارد: -1 جان

## 🔧 تنظیمات

### فایل‌های مهم:
- `game_config.json`: تنظیمات اصلی بازی
- `game_bot.db`: دیتابیس SQLite
- `card_images/`: تصاویر کارت‌ها

### تنظیمات ربات:
- Token ربات در `game_config.json`
- لیست ادمین‌ها در `admin_user_ids`

## 🚨 عیب‌یابی

### اگر ربات اجرا نمی‌شود:
1. بررسی کنید token ربات صحیح باشد
2. اطمینان حاصل کنید وابستگی‌ها نصب شده‌اند
3. فایل `check_db.py` را اجرا کنید

### اگر پنل وب اجرا نمی‌شود:
1. بررسی کنید Flask نصب شده باشد
2. پورت 5000 آزاد باشد
3. فایروال مشکلی ایجاد نکند

## 📊 مانیتورینگ

### لاگ‌ها:
- `bot.log`: لاگ‌های ربات تلگرام
- خروجی کنسول: لاگ‌های web API

### بررسی وضعیت:
- تعداد کاربران فعال
- آمار استفاده از کارت‌ها
- وضعیت Cooldown کارت‌ها

---
**نکته**: برای استفاده در production، از gunicorn یا uwsgi برای web API استفاده کنید.